{
  "name": "boredapes_simple",
  "license": "MIT-0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ boredapes_simple",
    "create-local": "graph create --node http://localhost:8020/ boredapes_simple",
    "remove-local": "graph remove --node http://localhost:8020/ boredapes_simple",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 boredapes_simple",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.53.0",
    "@graphprotocol/graph-ts": "0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
