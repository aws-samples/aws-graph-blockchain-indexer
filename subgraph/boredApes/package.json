{
  "name": "boredapes",
  "license": "MIT-0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ boredApes",
    "create-local": "graph create --node http://localhost:8020/ boredApes",
    "remove-local": "graph remove --node http://localhost:8020/ boredApes",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 boredApes",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.47.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
